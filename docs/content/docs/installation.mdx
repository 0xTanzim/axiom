---
title: Manual Installation
description: Complete guide to manually set up an Axiom project from scratch
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

# Manual Installation

This guide shows how to manually set up an Axiom project without using the archetype.

<Callout type="info">
**Quick start:** Use the [Maven archetype](/docs/getting-started#quick-start-with-maven-archetype) for automatic setup.
</Callout>

## Prerequisites

### Java 21 or Later

<Tabs items={['Windows', 'macOS', 'Linux']}>
  <Tab value="Windows">
**Download and Install:**
1. Download [Adoptium OpenJDK 21+](https://adoptium.net/temurin/releases/?os=windows&arch=x64&package=jdk&version=21) (Windows x64 MSI installer)
2. Run the installer
3. ✅ Check "Set JAVA_HOME" and "Add to PATH" during installation

**Verify:**
```cmd
java -version
# Should show: openjdk version "21" or higher
```

**Manual PATH setup (if needed):**
```cmd
setx JAVA_HOME "C:\Program Files\Eclipse Adoptium\jdk-21.0.x"
setx PATH "%PATH%;%JAVA_HOME%\bin"
```
  </Tab>
  <Tab value="macOS">
**Option 1: Homebrew (Recommended)**
```bash
brew install openjdk@21
```

**Option 2: SDKMAN!**
```bash
curl -s "https://get.sdkman.io" | bash
sdk install java 21-tem
```

**Option 3: Direct Download**
1. Download [Adoptium OpenJDK 21+](https://adoptium.net/temurin/releases/?os=mac&arch=x64&package=jdk&version=21) (macOS PKG)
2. Run the installer

**Verify:**
```bash
java -version
# Should show: openjdk version "21" or higher
```
  </Tab>
  <Tab value="Linux">
**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install openjdk-21-jdk
```

**Fedora/RHEL:**
```bash
sudo dnf install java-21-openjdk-devel
```

**SDKMAN! (Any Linux)**
```bash
curl -s "https://get.sdkman.io" | bash
sdk install java 21-tem
```

**Verify:**
```bash
java -version
# Should show: openjdk version "21" or higher
```
  </Tab>
</Tabs>

<Callout type="info">
- **Minimum:** Java 21 (virtual thread support)
- **Recommended:** Java 25 LTS
</Callout>

### Maven or Gradle

<Tabs items={['Maven', 'Gradle']}>
  <Tab value="Maven">
**Windows:**
1. Download [Maven 3.9+](https://maven.apache.org/download.cgi) (ZIP)
2. Extract to `C:\Program Files\Apache\maven`
3. Add to PATH:
   ```cmd
   setx PATH "%PATH%;C:\Program Files\Apache\maven\bin"
   ```

**macOS (Homebrew):**
```bash
brew install maven
```

**Linux:**
```bash
# Ubuntu/Debian
sudo apt install maven

# Fedora/RHEL
sudo dnf install maven
```

**Verify:**
```bash
mvn -version
# Should show: Apache Maven 3.9.x or higher
```
  </Tab>
  <Tab value="Gradle">
**Windows:**
1. Download [Gradle 8.5+](https://gradle.org/releases/) (ZIP)
2. Extract to `C:\Gradle`
3. Add to PATH:
   ```cmd
   setx PATH "%PATH%;C:\Gradle\bin"
   ```

**macOS (Homebrew):**
```bash
brew install gradle
```

**Linux:**
```bash
# SDKMAN! (recommended)
sdk install gradle

# Or manual download
curl -s "https://get.sdkman.io" | bash
sdk install gradle 8.5
```

**Verify:**
```bash
gradle -version
# Should show: Gradle 8.5 or higher
```
  </Tab>
</Tabs>

---

## Maven Setup

### 1. Create Project Structure

<Tabs items={['Windows (PowerShell)', 'Windows (CMD)', 'macOS/Linux']}>
  <Tab value="Windows (PowerShell)">
```powershell
mkdir my-axiom-app
cd my-axiom-app
mkdir -p src\main\java\com\example
mkdir -p src\main\resources
```
  </Tab>
  <Tab value="Windows (CMD)">
```cmd
mkdir my-axiom-app
cd my-axiom-app
mkdir src\main\java\com\example
mkdir src\main\resources
```
  </Tab>
  <Tab value="macOS/Linux">
```bash
mkdir my-axiom-app
cd my-axiom-app
mkdir -p src/main/java/com/example
mkdir -p src/main/resources
```
  </Tab>
</Tabs>

### 2. Create pom.xml

Create `pom.xml` in the project root:

```xml title="pom.xml"
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Your Project Coordinates -->
    <groupId>com.example</groupId>
    <artifactId>my-axiom-app</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>My Axiom App</name>
    <description>An Axiom web application</description>

    <!-- Java 21 Configuration (minimum required) -->
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <axiom.version>0.1.3</axiom.version>
    </properties>

    <!-- Dependencies -->
    <dependencies>
        <!-- Axiom Framework (THE ONLY dependency you need) -->
        <dependency>
            <groupId>io.github.0xtanzim</groupId>
            <artifactId>axiom</artifactId>
            <version>${axiom.version}</version>
            <type>pom</type>
        </dependency>
    </dependencies>

    <!-- Build Configuration -->
    <build>
        <plugins>
            <!-- Java Compiler -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- Run with: mvn exec:java -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <mainClass>com.example.App</mainClass>
                </configuration>
            </plugin>

            <!-- Package as executable JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.6.0</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>com.example.App</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
```

<Callout type="warn" title="Important">
Notice `<type>pom</type>` for the Axiom dependency. This is required because `axiom` is a meta package.
</Callout>

### 3. Create Application Class

Create `src/main/java/com/example/App.java`:

```java title="src/main/java/com/example/App.java"
package com.example;

import io.axiom.core.app.Axiom;
import io.axiom.core.routing.Router;
import java.util.Map;

public class App {
    public static void main(String[] args) {
        Router router = new Router();

        // Hello World
        router.get("/", ctx -> ctx.text("Hello, Axiom!"));

        // JSON endpoint
        router.get("/api/status", ctx ->
            ctx.json(Map.of(
                "status", "running",
                "framework", "Axiom"
            ))
        );

        // Path parameters
        router.get("/users/:id", ctx -> {
            String id = ctx.param("id");
            ctx.json(Map.of("id", id, "name", "User " + id));
        });

        // Start server
        Axiom.start(router, 8080);
        System.out.println("🚀 Server running at http://localhost:8080");
    }
}
```

### 4. Create Configuration (Optional)

Create `src/main/resources/application.properties`:

```properties title="src/main/resources/application.properties"
# Server Configuration
server.host=0.1.3.0
server.port=8080

# Application
app.name=my-axiom-app
app.debug=false
```

### 5. Run Your Application

<Tabs items={['Maven', 'Executable JAR']}>
  <Tab value="Maven">
**All Platforms:**
```bash
# Compile and run
mvn compile exec:java

# Server will start at http://localhost:8080
```
  </Tab>
  <Tab value="Executable JAR">
**Build:**
```bash
mvn package
```

**Run:**

<Tabs items={['Windows', 'macOS/Linux']}>
  <Tab value="Windows">
```cmd
java -jar target\my-axiom-app-1.0.0-SNAPSHOT.jar
```
  </Tab>
  <Tab value="macOS/Linux">
```bash
java -jar target/my-axiom-app-1.0.0-SNAPSHOT.jar
```
  </Tab>
</Tabs>
  </Tab>
</Tabs>

---

## Gradle Setup

### 1. Create Project Structure

<Tabs items={['Windows (PowerShell)', 'Windows (CMD)', 'macOS/Linux']}>
  <Tab value="Windows (PowerShell)">
```powershell
mkdir my-axiom-app
cd my-axiom-app
mkdir -p src\main\java\com\example
mkdir -p src\main\resources
```
  </Tab>
  <Tab value="Windows (CMD)">
```cmd
mkdir my-axiom-app
cd my-axiom-app
mkdir src\main\java\com\example
mkdir src\main\resources
```
  </Tab>
  <Tab value="macOS/Linux">
```bash
mkdir my-axiom-app
cd my-axiom-app
mkdir -p src/main/java/com/example
mkdir -p src/main/resources
```
  </Tab>
</Tabs>

### 2. Create build.gradle.kts

<Tabs items={['Kotlin DSL', 'Groovy DSL']}>
  <Tab value="Kotlin DSL">
```kotlin title="build.gradle.kts"
plugins {
    java
    application
}

group = "com.example"
version = "1.0.0-SNAPSHOT"

java {
    toolchain {
        languageVersion.set(JavaLanguageVersion.of(21))
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // Axiom Framework
    implementation("io.github.0xtanzim:axiom:0.1.3")
}

application {
    mainClass.set("com.example.App")
}

tasks.withType<JavaCompile> {
    options.compilerArgs.add("--enable-preview")
}

tasks.withType<JavaExec> {
    jvmArgs("--enable-preview")
}

tasks.jar {
    manifest {
        attributes["Main-Class"] = "com.example.App"
    }
    // Create fat JAR
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    from(configurations.runtimeClasspath.get().map { if (it.isDirectory) it else zipTree(it) })
}
```
  </Tab>
  <Tab value="Groovy DSL">
```groovy title="build.gradle"
plugins {
    id 'java'
    id 'application'
}

group = 'com.example'
version = '1.0.0-SNAPSHOT'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    // Axiom Framework
    implementation 'io.github.0xtanzim:axiom:0.1.3'
}

application {
    mainClass = 'com.example.App'
}

tasks.withType(JavaCompile) {
    options.compilerArgs << '--enable-preview'
}

tasks.withType(JavaExec) {
    jvmArgs '--enable-preview'
}

jar {
    manifest {
        attributes 'Main-Class': 'com.example.App'
    }
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
    from { configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) } }
}
```
  </Tab>
</Tabs>

### 3. Create Application Class

Same as Maven example above: `src/main/java/com/example/App.java`

### 4. Run Your Application

<Tabs items={['Windows', 'macOS/Linux']}>
  <Tab value="Windows">
```cmd
# Run directly
gradlew.bat run

# Or build JAR and run
gradlew.bat build
java -jar build\libs\my-axiom-app-1.0.0-SNAPSHOT.jar
```
  </Tab>
  <Tab value="macOS/Linux">
```bash
# Run directly
./gradlew run

# Or build JAR and run
./gradlew build
java -jar build/libs/my-axiom-app-1.0.0-SNAPSHOT.jar
```
  </Tab>
</Tabs>

---

## Project Structure

After setup, your project should look like this:

```
my-axiom-app/
├── pom.xml (or build.gradle.kts)
└── src/
    └── main/
        ├── java/
        │   └── com/
        │       └── example/
        │           └── App.java
        └── resources/
            └── application.properties
```

---

## Minimal Dependencies

Axiom is designed to be minimal. The `axiom` meta package brings:

| Module | Size | Purpose |
|--------|------|---------|
| `axiom-core` | ~100KB | Routing, middleware, lifecycle, config, DI, server |
| `axiom-persistence` | ~50KB | Optional database integration |

**Total:** ~150KB of framework code + SLF4J logging.

---

## Common Issues

### "SLF4J: No SLF4J providers were found"

**Warning shown:**
```
SLF4J(W): No SLF4J providers were found.
SLF4J(W): Defaulting to no-operation (NOP) logger implementation
```

**Cause:** Axiom uses SLF4J for logging but doesn't force a specific implementation.

**Solution:** Add a logging implementation:

<Tabs items={['slf4j-simple (easiest)', 'Logback (production)', 'Log4j2']}>
  <Tab value="slf4j-simple (easiest)">
```xml title="pom.xml"
<dependency>
    <groupId>org.slf4j</groupId>
    <artifactId>slf4j-simple</artifactId>
    <version>2.0.16</version>
</dependency>
```
  </Tab>
  <Tab value="Logback (production)">
```xml title="pom.xml"
<dependency>
    <groupId>ch.qos.logback</groupId>
    <artifactId>logback-classic</artifactId>
    <version>1.5.12</version>
</dependency>
```
  </Tab>
  <Tab value="Log4j2">
```xml title="pom.xml"
<dependency>
    <groupId>org.apache.logging.log4j</groupId>
    <artifactId>log4j-slf4j2-impl</artifactId>
    <version>2.24.3</version>
</dependency>
```
  </Tab>
</Tabs>

<Callout type="info">
**Recommended for development:** Use `slf4j-simple` — no configuration needed!
</Callout>

---

### "Could not find artifact axiom:jar"

**Solution:** Add `<type>pom</type>` to the dependency:
```xml
<dependency>
    <groupId>io.github.0xtanzim</groupId>
    <artifactId>axiom</artifactId>
    <version>0.1.3</version>
    <type>pom</type>  <!-- Required! -->
</dependency>
```

### "Unsupported class file version"

**Solution:** Ensure you're using Java 21 or later:
```bash
java -version  # Should show "openjdk version 21" or higher
```

---

## Next Steps

- **[Getting Started Guide](/docs/getting-started)** — Quick start tutorial
- **[Routing](/docs/routing)** — Define routes and handlers
- **[Configuration](/docs/config)** — Environment-based config
- **[Server Configuration](/docs/server)** — Running and deploying
